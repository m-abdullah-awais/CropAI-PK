# Don't re-verify/purge node_modules before every `pnpm run` (pnpm 11 default).
# The repo-root package.json + frontend workspace otherwise trips a no-TTY purge abort.
verify-deps-before-run=false
confirm-modules-purge=false
