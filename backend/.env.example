# Copy to .env to override defaults.
PORT=8077
# Comma-separated list of allowed CORS origins (the Next.js app).
ALLOWED_ORIGINS=http://localhost:3007,http://127.0.0.1:3007
# Override only if you move the data/ or models/ folders.
# DATA_DIR=../data
# MODEL_DIR=./models
